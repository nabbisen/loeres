schema_version = 3
fixture_id = "qp-linear-2d-trust-skip-001"
suite = "smoke"
problem_class = "box_quadratic_linear_inequalities"
solver_family = "constrained_projected_first_order"
dimension = 2
constraints = 2
scalar_family = "float"
validation_state = "trusted-by-caller"
conformance_groups = ["cluster-reference-smoke"]
variant = "solve"

# Under TrustedByCaller(FINITE) the pre-loop finite scans are skipped and the
# record says so; the answer is unchanged. Structural checks still run.

[config]
max_iterations = 5000
tolerance = 1e-12
step_scale = 1.0
projection_max_sweeps = 100000
projection_tolerance = 1e-13

[problem]
lower = [-10.0, -10.0]
upper = [10.0, 10.0]
initial = [0.0, 0.0]
quadratic_diag = [1.0, 1.0]
center = [5.3481, 4.7872]
constraint_matrix = [1.6, -1.3082, -0.0457, 0.9197]
constraint_rhs = [1.6013, -0.3988]

[expected]
status = "converged"
termination = "convergence-criterion"
solution = [0.673643, -0.400146]
error = "none"
violation_max = 1e-10

[tolerance]
solution_abs = 0.000001
solution_rel = 0.000001
