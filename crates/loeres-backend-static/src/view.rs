//! Borrowed fixed-size views over caller storage.
//!
//! Immutable and mutable views with dimension and stride metadata, so embedded users need not copy into Loeres-owned arrays.
//!
//! Defined by RFC 004 (baseline contiguous views; advanced views behind `static-views`). Lands in Milestone 2; this Phase 0 placeholder declares no public
//! items yet.
