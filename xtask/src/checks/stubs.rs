//! Registered-but-unimplemented gates from the RFC 010 / roadmap §5.4 blueprint.

pub fn run(name: &str) -> bool {
    eprintln!("[{name}] scaffold — implemented in a later milestone (see RFC 010). SKIPPED.");
    true
}
